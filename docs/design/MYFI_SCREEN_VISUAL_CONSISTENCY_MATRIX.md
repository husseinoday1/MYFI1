# MYFI — Screen Visual Consistency Matrix

**Registered:** 2026-08-25 · **Status:** CANONICAL
**Basis:** code audit at HEAD `d2ed3ae03c137d818040dfe77c665c516b8440b7` +
`MYFI_APPROVED_VISUAL_REFERENCE_REGISTER.md` (REF-01 through REF-07).
Financial-data impact for every row in this matrix is **NONE** — this is a
presentation/navigation document only.

| Screen | Current state | Approved target | Ref | Identity alignment | Component alignment | UX gap | RTL | A11y | Light/Dark | Design debt | Reusable logic | Reusable UI | Legacy UI | Migration complexity | Priority |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| **Home** | `HomeScreen.js`, 1,348 lines, active, primary tab | Home (primary tab, unchanged position) | REF-01 | Close — hero card, wallets, quick add, recent list all present | High — `WalletBalanceCard`, `AddTransModal`, `EntryContextRow` reusable | Bottom-nav must move from 5→4 items; verify period-pill control matches REF-01 exactly | Present (`rowDirFor`/`textAlignFor`) | Not evaluated | Confirmed sound (one structure) | Magic-value density (fontSize/padding literals) | High | High | None | Low-Medium | P1 |
| **My Money** | **Does not exist as a screen** | New hub: 4 gateway cards → Wallets, History, Budget, Reports | REF-04 | New build, but content largely assembles existing screens | Needs new `GatewayCard` primitive; reuses `HistoryScreen`/`ReportsScreen` as destinations | Full IA build: hub + possibly new Wallets-detail and Budget-detail screens (Budget currently has no confirmed dedicated screen) | Must inherit existing RTL infra | Not evaluated | Must follow existing 2-theme structure | N/A (new) | Assembles existing logic (History, Reports, wallet data already on Home) | Partial (History/Reports screens reusable as destinations) | None | Medium-High | P1 |
| **Follow-ups** | `TrackersLabScreen.js`, 1,448 lines, active, primary tab (`trackers`) | Follow-ups (primary tab, relabeled) | REF-05 | Already close — debts/goals/commitments unified; add Payment History as explicit section, confirm Bills/Installments/Subscriptions coverage | High — Quick Add row already matches (Debt/Receivable/Commitment/Goal); confirm `NewItemModal` covers all 4 types | Relabel tab; verify all 6 REF-05 sections (incl. Payment History, Installments, Subscriptions) exist or need adding | Not exhaustively verified this pass | Not evaluated | Not exhaustively verified this pass | Magic-value density (shared with Home) | High | High (module-filtered list already built) | None | Low-Medium | P1 |
| **More** | **Does not exist as a screen** | New hub: My Tools / Data & Files / Benefits / Help / Settings + 3 customizable shortcuts | REF-06 | New build; content assembles pieces currently inside Settings (`DataPage`, `GuidePage`, `SupportPage`, `AboutPage`) | Needs new `SectionListRow`, shortcut-customization UI | Relocate `DataPage`/`GuidePage`/`SupportPage`/`AboutPage` out of Settings into More; build "Benefits" (no current equivalent found) and "My Tools" (assembles Categories/Currencies/Templates/Archive/Feature-visibility, currently scattered) | Must inherit existing RTL infra | Not evaluated | Must follow 2-theme structure (REF-06 supplies both) | N/A (new) | Partial — Data/Help/About logic exists, just mis-routed | Partial | None | Medium-High | P1 |
| **Settings** | `SettingsScreen.js` (2,391 lines) + embedded `SettingsLegacyScreen.js` (3,235 lines), active, primary tab | Reached from More; exactly 5 root sections | REF-07 | Root row count already matches target (5) once Data/Guide/Support/About are relocated to More | High risk — must migrate `SettingsScreen`+`SettingsLegacyScreen` as one unit (Legacy is live, not dead, embedded at `SettingsScreen.js:1172,1176,1680`) | **RESOLVED (2026-08-26):** Country and Base Currency are approved under Financial Preferences, not Appearance & Language (current code groups Country/Currency with Language/Theme under `T.general`, `SettingsScreen.js:1402-1403` — this is now a confirmed code-vs-target gap to close during implementation, not an open design question). Accent-color picker, payment-methods row, VAT/rounding row are **REJECTED FOR CURRENT TARGET / REMOVE FROM TARGET** — settled, not flagged | Not exhaustively verified this pass | Not evaluated | REF-07 supplies both themes | Highest single-screen debt in the app (5,600+ combined lines, live legacy entanglement) | High (auth, deletion, sync logic all here) | Partial | `SettingsLegacyScreen.js` — **live, not disposable** | High | P1 (highest-risk migration) |
| **History** | `HistoryScreen.js`, 940 lines, active, primary tab | Secondary destination (My Money → Transactions & History) | REF-04 (as My Money sub-screen) | Close — filter tabs, search, date grouping already present | High — reusable near-as-is | Becomes a sub-destination rather than a tab; verify search-in-history-only rule (Blueprint §4) already holds — confirmed, no permanent Home search control found | Confirmed (`writingDirectionFor`, `HistoryScreen.js:15`) | Not evaluated | Not exhaustively verified this pass | Magic-value density | High | High | None | Low | P2 |
| **Reports** | `ReportsScreen.js`, 2,211 lines, active, primary tab | Gateway inside My Money | REF-04 (Reports & Analytics preview) | Largest screen; content depth (charts, filters, export) exceeds what a "gateway" summary shows — the full screen persists as the destination, only its tab position changes | Needs canonical Chart Design System (color-sourcing rule, see Design System §12); local `CHART_COLORS` to be replaced | Largest migration scope of any existing screen; own local color palette to reconcile | Not exhaustively verified this pass | Not evaluated | Not exhaustively verified this pass | Own local `CHART_COLORS`, high magic-value density | Medium (charts logic reusable, only color-sourcing changes) | Partial | None | High | P2 |
| **Archive** | `ArchiveScreen.js`, 898 lines, active, only reachable via Settings | **More → My Tools → Archive** (per Product Owner correction 3.1 — supersedes this audit's earlier "secondary from Home/My Money" suggestion) | REF-06 (My Tools section) | Content (archived/closed trackers, closed years) matches My Tools' "Archive" row description | Reusable as-is | Relocate entry point only — from Settings' `onOpenArchive` wiring to My Tools | Not exhaustively verified this pass | Not evaluated | Not exhaustively verified this pass | None beyond relocation | High (content/logic unaffected) | High | None | Low | P2 |
| **Onboarding** | `OnboardingScreen.js`, 390 lines, active | **APPROVED (2026-08-26), exact 6-step order:** 1) Welcome, 2) What matters to you first, 3) Customize your experience, 4) Create first wallet, 5) Privacy first, 6) Everything is ready | REF-02(1), REF-03(2), REF-03B(3), REF-03D(4), REF-03C(5), REF-03E(6) | Approved as intentionally short — step-count question is **resolved, not open**. Current flow has local-first messaging already | Needs `SelectorRow` (customize step), `MultiSelect` (priorities step, already exists) | REF-03D's source "1 of 6" mislabel is corrected by this ruling — its approved canonical position is step 4. **New target rules to verify against current `OnboardingScreen.js` during implementation:** no Personal/Business/Dual account-type selection; no opening-balance requirement; permissions requested contextually per-feature, not during onboarding; existing users get a short "What Changed" flow instead of full onboarding (none of this confirmed present or absent in `OnboardingScreen.js` in this pass — implementation-phase verification item) | Arabic-only in all references (no English onboarding reference supplied) | Not evaluated | Only Light shown in every onboarding reference — Dark onboarding not yet verified against any approved image | None beyond the noted implementation-phase verification | Medium | Partial | None | Medium | P2 |
| **CommitScreen / DebtsScreen / GoalsScreen / AuthScreen / SpaceScreen** | Orphaned, unreferenced by `App.js` (75/356/347/361/184 lines) | None — retirement candidates | — | N/A | N/A | Explicit disposition needed (see Component Inventory) | N/A | N/A | N/A | Dead-code debt | Low (superseded already) | None | All five | Low (once approved) | P3 |

**Financial-data impact for every row above: NONE.** This matrix records
navigation/visual/component classification only; no ledger, balance,
transaction, schema, or sync behavior is described or altered.
