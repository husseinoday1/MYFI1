# MYFI — إغلاق Phase 12: استعادة تعارض المزامنة V2 والمسار البديل

**الحالة: 11 commit محليًا على `fix/pui-001-r2-onboarding-reader-recent-transactions`،
بوابة الجودة 154 passed / 0 failed / 11 skipped. لا push، ولا بناء، ولا أي إجراء
على الجهاز الحقيقي حتى تاريخ هذا المستند.**

## ما الذي كان معطلًا فعلًا

جهاز المستخدم الحقيقي عَلِق بعد محاولة "استبدال النسخة المحلية" في Phase 12:
الترقية اكتملت محليًا لكن التفعيل لم يكتمل، فصارت البيانات الحية لقطة bootstrap
مجمَّدة من 2026-08-31، بينما بيانات المستخدم الحقيقية محفوظة في checkpoint سليم.
التفاصيل والأدلة الأولية في
`MYFI_CLAUDE_REVIEW_V2_CONFLICT_RECOVERY_STALE_BOOTSTRAP_2026-09-02.md`.

تتبّع الكود كشف أن الجهاز كان محكومًا بثلاثة أقفال مستقلة، لا قفل واحد:

1. **سلسلة shadow.** `v2PreflightMutation` يقارن مراجعة الكيان بالجدول الحي
   (`financialLedgerV7Repository.js:3194`)، ويُستدعى في وضعي shadow والإنتاج معًا،
   بينما shadow لا يطبّق شيئًا. فالأمر الأول من السلسلة يمر ويبقى الجدول ثابتًا،
   والثاني يفشل بـ`financial_v2_remote_cas_conflict`. **أي جهاز متأخر بمراجعتين
   فأكثر على أي كيان واحد لا يستطيع تفعيل V2 أبدًا** — خلل معماري عام لا يخص
   جهازنا.
2. **تراكم `ledger_outbox_v2`.** يُكتب كلما كان V2 غير مفعَّل
   (`shouldWriteLegacyOutboxV1`)، والترقية تركت `activated_at` فارغًا. الصفوف ليست
   خاملة: مزامنة V1 تبني الحمولة من `payload_json` مباشرة بلا رجوع للجداول الحية،
   فكانت سترفع كيانات أزالتها الاستعادة للتو.
3. **tombstone دائم.** فشل الـpreflight يكتب `apply_status='conflict'` في
   `ledger_inbox_v3`، وهذه الحالة تُفحص **قبل** الـpreflight ويحفظها كل كاتب
   صراحةً، ولا شيء في الكود يمسحها. فالأمر الذي فشل مرة لا يُعاد التحقق منه أبدًا
   ولو أُصلح سبب فشله. **هذا وحده كان سيُفشل التجربة على الجهاز بخطأ مختلف،
   ولأوهمنا أن إصلاح shadow لم ينجح.**

## ما نُفِّذ

| Commit | المحتوى |
|---|---|
| `09bcd1a` | زر الاستعادة في شاشة التشخيص، مشروط بالحالة التي يُصلحها |
| `34695d5` | قراءة صفوف `outbox_v2` كاملة + `checkpoint.createdAt` (قراءة فقط) |
| `e493578` | `discardLegacyOutboxAfterCheckpointRestoreV1` |
| `7d3b715` | إسقاط سلسلة shadow داخل التمريرة |
| `c973d88` | حمل الإسقاط عبر صفحات الدورة الواحدة |
| `51b511b` | `resetFinancialV2ShadowValidationStateV8` |
| `bc71c68` | وصل التصفير داخل مسار التفعيل العادي |
| `f0ec002` | خطوات الإصلاح الثلاث في الشاشة + `retireConflictRecoveryIntentAfterActivationV1` |
| `badcb8c` | `financialV2StaleCommandRecoveryV1.js` — المسار الضيق |
| `349ea50` | تفضيل المسار الضيق في `prepareV2ConflictRecovery` + نص التأكيد |
| `c022d6b` | تصحيح نص "نقطة الرجوع المحلية" |

## البدائل المرفوضة، ولماذا

- **تخفيف فحص الـCAS في وضع shadow** (اقتراح أصلي): مرفوض. الفحص صحيح ويحمي
  البيانات؛ المشكلة أن shadow يستخدم فحصًا يفترض التطبيق وهو ممنوع من التطبيق.
  الإسقاط يصحّح الفرضية بدل إضعاف الفحص، والحالات السلبية في الاختبار تُثبت أن
  فجوة حقيقية في السلسلة لا تزال تُرفض.
- **بذر الإسقاط من `ledger_inbox_v3`** (تصميم أُقر ثم سقط): غير قابل للتنفيذ —
  الجدول لا يحفظ `entity_type` ولا `entity_id` ولا `revision`. تنفيذه يستلزم تعديل
  مخطط SQLite مع migration، وهو ما رُفض لفجوة نادرة (انقطاع دورة في منتصف
  السلسلة). البديل المنفَّذ: تصفير حالة التحقق عند بدء كل محاولة.
- **إبقاء مسار bootstrap للحالة الضيقة**: مرفوض. اللقطة مجمَّدة بطبيعتها، فاستبدال
  جهاز حيّ بها يمحو ما فعله الطرفان بعدها. المسار الجديد لا يستبدل شيئًا.
- **ربط المسار الضيق بالواجهة**: مرفوض لصالح التفريع في الـaction، فيستفيد كل
  مستخدم مستقبلي من الزر القائم بلا تغيير واجهة.
- **رفع حاجز المزامنة بعد الاستعادة**: مرفوض. التعارض الأصلي يبقى قائمًا بعد
  الاستعادة، فالحاجز يبقى مغلقًا حتى تفعيل مُثبت فعليًا من قاعدة البيانات.

## الأثر المالي

```
Financial Data: NONE مباشرة — لا INSERT/UPDATE/DELETE جديد على أي جدول مالي في
                الـdiff. الاستعادة وحدها تُعيد ملء الجداول المالية، وعبر مساعدي
                النسخ المُدقَّقين القائمين، بأعداد مُثبتة مطابقة لإيصال الـcheckpoint
                قبل الكتابة وبعدها.
SQLite Schema:  NONE — FINANCIAL_SQLITE_SCHEMA_VERSION بلا تغيير، ولا CREATE/ALTER،
                ولا ملف migration ممسوس.
Migration Required: NO
Existing User Data: PRESERVED — عمليات الحذف الثلاث الجديدة كلها على جداول النقل
                (ledger_outbox_v2 / outbox_v3 / inbox_v3 بحالة conflict فقط).
                لا معنى ولا دقة ولا وحدة قيمة مالية تتغيّر؛ الأرصدة تبقى مشتقة من
                postings لأن النسخ يشمل postings حرفيًا؛ التحويلات وFX التاريخي
                وصيغة النسخ الاحتياطي خارج نطاق التغيير كليًا.
Proof: run-p20-v2-conflict-recovery-restore / -legacy-outbox-discard /
       -shadow-sequential-commands / -shadow-validation-reset /
       -conflict-intent-retire / -stale-command-repair / -conflict-recovery-resume،
       وp19-011-controlled-v2-activation، وبوابة الجودة كاملة.
```

## إثبات التكرار

كل ما يحمل حالة (عدّاد، مراجعة، آلة حالات) له اختبار ينفّذه مرتين متتابعتين:
الاستعادة تُرفض في المرة الثانية؛ الحذف يصبح no-op بلا مساس بالأدلة؛ التقاعد يُرفض
بعد وقوعه؛ ودورة "فشل ← تصفير ← إعادة محاولة تنجح" منفَّذة بالكامل على قاعدة واحدة.

## مراجعة ما قبل الدفع

`/code-review high` على المدى `origin/…..HEAD` أخرج نتيجتين، وكلتاهما أُصلحتا قبل
طلب الدفع:

1. **زر التفعيل كان يعلن النجاح اعتمادًا على التفعيل وحده** — لو نجح التفعيل وفشل
   تقاعد الـintent، لأُبلغ المستخدم أن المزامنة عادت بينما الحاجز لا يزال مغلقًا
   ويرفض كل مزامنة تالية. صار النجاح مشروطًا بالاثنين معًا، مع رسالة صريحة للحالة
   الوسطى.
2. **`canActivate` كان ينقصه حارس `activeNamespace`** الموجود في شقيقيه، فكان
   يمكن أن يمرّر `undefined` فيقع على الافتراضي `'guest'`.

## الحدود المعروفة (معلنة، غير مُصلَحة عمدًا)

- إسقاط shadow يعيش لدورة مزامنة واحدة. دورة تنقطع في منتصف السلسلة تترك المؤشر
  فوق الجداول، ويعالج ذلك التصفير عند بدء المحاولة التالية لا الاستئناف.
- كتابة tombstone دائم من وضع لا يكتب شيئًا تبقى مشكلة تصميمية أعمق، أُجِّلت
  لجولة مستقلة لأنها تمسّ مسار الإنتاج المشترك.
- الاستعادة لا تُعيد `activated_at`؛ إعادة التفعيل تمر بمسارها الكامل الموثق.

## ما لم يحدث بعد

لا push، ولا بناء APK، ولا أي إجراء على الجهاز الحقيقي. الترتيب المطلوب على الجهاز
عند الموافقة: استعادة ← إعادة تشغيل التطبيق ← إزالة المعلّق ← تشغيل المزامنة ←
إعادة تشغيل. خطوة التفعيل وحدها هي التي تكتب على بيانات مالية حقيقية؛ الاستعادة
تنسخ من checkpoint مُثبت، والإزالة لا تمسّ بيانات مالية إطلاقًا.
