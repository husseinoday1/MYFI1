# MYFI — إغلاق Phase 12: استعادة تعارض المزامنة V2 والمسار البديل

**الحالة النهائية (2026-09-03): مُغلقة ومؤكَّدة على الجهاز الحقيقي بالكامل، بما فيها
تنظيف صفوف outbox_v2 الثلاثة. البند الوحيد المتبقي مؤجَّل بقرار خادم مستقل — انظر
قسم "RESOLVED" وقسم "بنود مفتوحة" أدناه. الفقرة التالية أُبقيت كسجل تاريخي لحظة
كتابتها ولم تعد تصف الحالة الحالية.**

**الحالة عند الكتابة الأولى: 11 commit محليًا على
`fix/pui-001-r2-onboarding-reader-recent-transactions`، بوابة الجودة
154 passed / 0 failed / 11 skipped. لا push، ولا بناء، ولا أي إجراء على الجهاز
الحقيقي حتى تاريخ هذا المستند.**

## ما الذي كان معطلًا فعلًا

جهاز المستخدم الحقيقي عَلِق بعد محاولة "استبدال النسخة المحلية" في Phase 12:
الترقية اكتملت محليًا لكن التفعيل لم يكتمل، فصارت البيانات الحية لقطة bootstrap
مجمَّدة من 2026-08-31، بينما بيانات المستخدم الحقيقية محفوظة في checkpoint سليم.
التفاصيل والأدلة الأولية في
`MYFI_CLAUDE_REVIEW_V2_CONFLICT_RECOVERY_STALE_BOOTSTRAP_2026-09-02.md`.

تتبّع الكود كشف أن الجهاز كان محكومًا بثلاثة أقفال مستقلة، لا قفل واحد:

1. **سلسلة shadow.** `v2PreflightMutation` يقارن مراجعة الكيان بالجدول الحي
   (`financialLedgerV7Repository.js:3194`)، ويُستدعى في وضعي shadow والإنتاج معًا،
   بينما shadow لا يطبّق شيئًا. فالأمر الأول من السلسلة يمر ويبقى الجدول ثابتًا،
   والثاني يفشل بـ`financial_v2_remote_cas_conflict`. **أي جهاز متأخر بمراجعتين
   فأكثر على أي كيان واحد لا يستطيع تفعيل V2 أبدًا** — خلل معماري عام لا يخص
   جهازنا.
2. **تراكم `ledger_outbox_v2`.** يُكتب كلما كان V2 غير مفعَّل
   (`shouldWriteLegacyOutboxV1`)، والترقية تركت `activated_at` فارغًا. الصفوف ليست
   خاملة: مزامنة V1 تبني الحمولة من `payload_json` مباشرة بلا رجوع للجداول الحية،
   فكانت سترفع كيانات أزالتها الاستعادة للتو.
3. **tombstone دائم.** فشل الـpreflight يكتب `apply_status='conflict'` في
   `ledger_inbox_v3`، وهذه الحالة تُفحص **قبل** الـpreflight ويحفظها كل كاتب
   صراحةً، ولا شيء في الكود يمسحها. فالأمر الذي فشل مرة لا يُعاد التحقق منه أبدًا
   ولو أُصلح سبب فشله. **هذا وحده كان سيُفشل التجربة على الجهاز بخطأ مختلف،
   ولأوهمنا أن إصلاح shadow لم ينجح.**

## ما نُفِّذ

| Commit | المحتوى |
|---|---|
| `09bcd1a` | زر الاستعادة في شاشة التشخيص، مشروط بالحالة التي يُصلحها |
| `34695d5` | قراءة صفوف `outbox_v2` كاملة + `checkpoint.createdAt` (قراءة فقط) |
| `e493578` | `discardLegacyOutboxAfterCheckpointRestoreV1` |
| `7d3b715` | إسقاط سلسلة shadow داخل التمريرة |
| `c973d88` | حمل الإسقاط عبر صفحات الدورة الواحدة |
| `51b511b` | `resetFinancialV2ShadowValidationStateV8` |
| `bc71c68` | وصل التصفير داخل مسار التفعيل العادي |
| `f0ec002` | خطوات الإصلاح الثلاث في الشاشة + `retireConflictRecoveryIntentAfterActivationV1` |
| `badcb8c` | `financialV2StaleCommandRecoveryV1.js` — المسار الضيق |
| `349ea50` | تفضيل المسار الضيق في `prepareV2ConflictRecovery` + نص التأكيد |
| `c022d6b` | تصحيح نص "نقطة الرجوع المحلية" |

## البدائل المرفوضة، ولماذا

- **تخفيف فحص الـCAS في وضع shadow** (اقتراح أصلي): مرفوض. الفحص صحيح ويحمي
  البيانات؛ المشكلة أن shadow يستخدم فحصًا يفترض التطبيق وهو ممنوع من التطبيق.
  الإسقاط يصحّح الفرضية بدل إضعاف الفحص، والحالات السلبية في الاختبار تُثبت أن
  فجوة حقيقية في السلسلة لا تزال تُرفض.
- **بذر الإسقاط من `ledger_inbox_v3`** (تصميم أُقر ثم سقط): غير قابل للتنفيذ —
  الجدول لا يحفظ `entity_type` ولا `entity_id` ولا `revision`. تنفيذه يستلزم تعديل
  مخطط SQLite مع migration، وهو ما رُفض لفجوة نادرة (انقطاع دورة في منتصف
  السلسلة). البديل المنفَّذ: تصفير حالة التحقق عند بدء كل محاولة.
- **إبقاء مسار bootstrap للحالة الضيقة**: مرفوض. اللقطة مجمَّدة بطبيعتها، فاستبدال
  جهاز حيّ بها يمحو ما فعله الطرفان بعدها. المسار الجديد لا يستبدل شيئًا.
- **ربط المسار الضيق بالواجهة**: مرفوض لصالح التفريع في الـaction، فيستفيد كل
  مستخدم مستقبلي من الزر القائم بلا تغيير واجهة.
- **رفع حاجز المزامنة بعد الاستعادة**: مرفوض. التعارض الأصلي يبقى قائمًا بعد
  الاستعادة، فالحاجز يبقى مغلقًا حتى تفعيل مُثبت فعليًا من قاعدة البيانات.

## الأثر المالي

```
Financial Data: NONE مباشرة — لا INSERT/UPDATE/DELETE جديد على أي جدول مالي في
                الـdiff. الاستعادة وحدها تُعيد ملء الجداول المالية، وعبر مساعدي
                النسخ المُدقَّقين القائمين، بأعداد مُثبتة مطابقة لإيصال الـcheckpoint
                قبل الكتابة وبعدها.
SQLite Schema:  NONE — FINANCIAL_SQLITE_SCHEMA_VERSION بلا تغيير، ولا CREATE/ALTER،
                ولا ملف migration ممسوس.
Migration Required: NO
Existing User Data: PRESERVED — عمليات الحذف الثلاث الجديدة كلها على جداول النقل
                (ledger_outbox_v2 / outbox_v3 / inbox_v3 بحالة conflict فقط).
                لا معنى ولا دقة ولا وحدة قيمة مالية تتغيّر؛ الأرصدة تبقى مشتقة من
                postings لأن النسخ يشمل postings حرفيًا؛ التحويلات وFX التاريخي
                وصيغة النسخ الاحتياطي خارج نطاق التغيير كليًا.
Proof: run-p20-v2-conflict-recovery-restore / -legacy-outbox-discard /
       -shadow-sequential-commands / -shadow-validation-reset /
       -conflict-intent-retire / -stale-command-repair / -conflict-recovery-resume،
       وp19-011-controlled-v2-activation، وبوابة الجودة كاملة.
```

## إثبات التكرار

كل ما يحمل حالة (عدّاد، مراجعة، آلة حالات) له اختبار ينفّذه مرتين متتابعتين:
الاستعادة تُرفض في المرة الثانية؛ الحذف يصبح no-op بلا مساس بالأدلة؛ التقاعد يُرفض
بعد وقوعه؛ ودورة "فشل ← تصفير ← إعادة محاولة تنجح" منفَّذة بالكامل على قاعدة واحدة.

## مراجعة ما قبل الدفع

`/code-review high` على المدى `origin/…..HEAD` أخرج نتيجتين، وكلتاهما أُصلحتا قبل
طلب الدفع:

1. **زر التفعيل كان يعلن النجاح اعتمادًا على التفعيل وحده** — لو نجح التفعيل وفشل
   تقاعد الـintent، لأُبلغ المستخدم أن المزامنة عادت بينما الحاجز لا يزال مغلقًا
   ويرفض كل مزامنة تالية. صار النجاح مشروطًا بالاثنين معًا، مع رسالة صريحة للحالة
   الوسطى.
2. **`canActivate` كان ينقصه حارس `activeNamespace`** الموجود في شقيقيه، فكان
   يمكن أن يمرّر `undefined` فيقع على الافتراضي `'guest'`.

## الحدود المعروفة (معلنة، غير مُصلَحة عمدًا)

- إسقاط shadow يعيش لدورة مزامنة واحدة. دورة تنقطع في منتصف السلسلة تترك المؤشر
  فوق الجداول، ويعالج ذلك التصفير عند بدء المحاولة التالية لا الاستئناف.
- كتابة tombstone دائم من وضع لا يكتب شيئًا تبقى مشكلة تصميمية أعمق، أُجِّلت
  لجولة مستقلة لأنها تمسّ مسار الإنتاج المشترك.
- الاستعادة لا تُعيد `activated_at`؛ إعادة التفعيل تمر بمسارها الكامل الموثق.

---

# RESOLVED — 2026-09-03، مُثبت على الجهاز الحقيقي

**تصحيح لما سبق في هذه الوثيقة:** القسم الأخير كان يقول «لا push ولا بناء ولا إجراء
على الجهاز». صار ذلك قديمًا في نفس اليوم. الحالة النهائية أدناه.

## ما أُثبت فعليًا

- **التفعيل نجح:** `activated.ok:true` عند `2026-09-03T07:11:08.380Z`،
  و`activeProtocolVersion: 2`.
- **الحاجز ارتفع بدليل من قاعدة البيانات لا بادعاء واجهة:**
  `intentRetired: {ok:true, status:'retired_after_reviewed_repair'}` —
  `retireConflictRecoveryIntentAfterActivationV1` تقرأ `activated_at` بنفسها.
- **اللحاق تقارب بلا تعارض:** `productionCatchup` نزّل 13 أمرًا وطبّق **صفرًا** —
  كلها مرّت عبر `v2AlreadyAppliedLocally`. ثم تمريرة ثانية بصفر تنزيل (سكون).
- **جولة الذهاب أُثبتت عمليًا لا نظريًا:** أعاد المالك إنشاء محفظة Sh2 والرصيد
  الافتتاحي 100$ والمصروف 9$ — رُفعت الثلاثة بصفر تعارض،
  `outboxV3PendingCount: 0` و`lastSyncError: null`. وهذا **قياس** لمراجعة
  workspace المحلية لا استنتاج: لو كانت متأخرة عن السحابة لرُفض أول رفع بنفس
  `financial_v2_revision_conflict` الأصلي.

البناء: CI `33725458416` أخضر، `BuildCommit=aad006548e38a20d2e782ed730904c39c621160c`
مطابقًا لـHEAD، APK SHA-256 `947b7fdb…2052`.

## الأعطال الخمسة التي كانت تمنع الإغلاق فعلًا

كان التشخيص الأول يفترض عطلًا واحدًا. كانت خمسة، كلٌّ يخفي التالي:

1. **لقطة bootstrap مجمَّدة** حلّت محل البيانات الحية (التشخيص الأصلي).
2. **سلسلة shadow:** `v2PreflightMutation` يقارن بالجدول الحي بينما shadow لا
   يطبّق ⇒ أي جهاز متأخر بمراجعتين فأكثر مقفل عن التفعيل دائمًا.
3. **الإسقاط لا يعبر الصفحات** ⇒ لحاق يتجاوز صفحة واحدة يعود للقفل.
4. **tombstone دائم** في `ledger_inbox_v3`: أمر فشل مرة لا يُعاد التحقق منه أبدًا،
   ولا شيء في الكود يمسحه. هذا وحده كان سيُفشل التجربة بخطأ مختلف ويوهمنا أن
   إصلاح shadow لم ينجح.
5. **تاريخ يُعاد تشغيله من الصفر على دفتر ليس في أوله:** لا آلية تبذر المؤشر بعد
   bootstrap، واستجابة السحابة لا تحمل رقم تسلسل أصلًا.

## البدائل المرفوضة (إضافة لما سبق)

- **بذر المؤشر من الـbootstrap** — الأصحّ معماريًا، **وغير قابل للتنفيذ**:
  `normalizeLedgerRegistration` لا يستخرج أي رقم تسلسل؛ يحتاج تغييرًا في الخادم.
- **قبول تساوي المراجعة وحده كدليل «مُطبَّق أصلًا»** — مرفوض: ترجيح لا برهان على
  بيانات مالية. اعتُمد تطابق `canonicalSyncValue` بدلًا منه، وأي انحراف مستقبلي
  بين الكاتب وإعادة البناء يجعل السلوك **أشدّ** (تعارض) لا أضعف.
- **إعادة تشغيل تلقائية لصفوف outbox_v2 لاسترداد الحركات** — مرفوضة لصالح إعادة
  إدخال يدوية: ثلاثة عناصر مقابل آلية غير مُختبرة على بيانات مالية.

## بنود مفتوحة — ثلاثة أُغلقت 2026-09-03، وواحد يبقى

### ✅ مُغلق — الـtombstone الدائم (`ddfa8ef`)
كان **الأخطر**، وأشد مما وُصف أولًا. فشل preflight يسجّل الأمر `conflict` في
`ledger_inbox_v3`، والفحص يقع **قبل** أي معالجة ويعود للمجموعة كلها، **والمؤشر لا
يتقدّم عن مجموعة مرفوضة** — فأمر واحد كان يوقف كل ما خلفه. ولا مسار يمسحه بعد
التفعيل: التصفير يرفض على دفتر مفعَّل، والبديلان الوحيدان ترقية كاملة أو إعادة ضبط
محلية. أي **توقف كامل ودائم لتنزيل المزامنة** من تعارض عابر واحد.

العلامة صارت دليلًا لا بوابة: إعادة التحقق نفسها هي فحص التقادم. تعارض قائم يفشل
ثانيةً **بأرقامه الحية** بدل رمز مخزَّن أعمى، وتعارض زالت أسبابه يمر. واكتشف
الاختبار خللًا ثانيًا لم يكن في التصميم: `CASE` كانت تحفظ `conflict` فوق **تطبيق
حقيقي**، فأمرٌ طُبِّق فعلًا يبقى مسجَّلًا متعارضًا للأبد — سجل كاذب يمنع اختصار
«مُطبَّق سلفًا» من التعرّف عليه.

### ✅ مُغلق — الحذف غير المحدود في الاستعادة (`49fae2e`, `7f4f5a1`)
**القطبية معكوسة عن الحدس:** صف كُتب **قبل** `checkpoint.createdAt` نتيجته داخل
الـcheckpoint فتعود مع الاستعادة، ويبقى رفعًا معلّقًا صالحًا — **يجب أن ينجو**،
وكان يُتلَف. وصف كُتب **بعده** أزالت الاستعادة أثره، فبقاؤه يجعل الطابور يدّعي
تغييرات لم تعد موجودة — يُحفظ حرفيًا في الـintent ثم يُحذف. **الرفض كان الجواب
الخطأ هنا** (بعكس دالة الحذف القديمة): يترك الطابور متناقضًا مع الدفتر المستعاد.

والمراجعة أمسكت خللًا في الإصلاح نفسه: تضييقه إلى الصفوف غير المُقرّة كان يُبقي
صفًا مُقرًّا بعد الحد، و`v2ExactLocalEcho` يطابق بالـ`mutation_id` وحده — فإعادة
تشغيل ذلك الأمر تدخل فرع الـecho وتفشل **قبل** فحص «مُطبَّق سلفًا» الذي كان سيحلّها.

### ✅ مُغلق — صفوف `outbox_v2` المالية (`0b1ad54`, `0c9aa0c`)
الإزالة تُعرض فقط حيث تكون صادقة: **بعد التفعيل** لا يقرأ مسار المزامنة الطابور
القديم ولا يكتب فيه، فما بقي لا يمكن أن يصل السحابة؛ وقبله قد يكون عملًا معلّقًا
حقيقيًا فالرفض كامل.

والتأكيد **سجل لا ملاحظة**: إقرار منفصل لكل صف (فهي حركات مالية مستقلة، وضغطة
واحدة تغطيها كلها كانت ستُتلف عملًا لم يقارنه المالك)، مع بصمة **نصية حرفية** —
أشد من المقارنة القانونية لأن أي إعادة كتابة تُبطل الإقرار. والقراءة والبصم داخل
المعاملة، والصفوف تُحفظ قبل الحذف.

> **شرط بشري لا يستطيع الكود فرضه:** لا تُستخدم هذه البطاقة قبل أن يتأكد المالك أن
> ما أعاد إدخاله يطابق الحمولات المحفوظة. الحارس يمنع الحذف بلا إقرار، لكنه لا
> يتحقق من صحة المقارنة.

**مؤكَّد على الجهاز الحقيقي (بناء `1d26236`):** راجع المالك الصفوف الثلاثة يدويًا
(فرق طفيف لوحظ في الصف #68، غير جوهري)، أقرّها واحدًا واحدًا، وضغط الحذف. النتيجة:
`discarded:3`، `outboxV2PendingCount:0`، `lastSyncError:null`، `online:true`. لا أثر
باقٍ من حادثة 08-31 على هذا الجهاز.

### ⏸ يبقى مفتوحًا — كيان تاريخه أطول من أمر واحد
التخطي يشترط `currentRevision === item.revision`، فكيان يقف محليًا عند مراجعة
متقدمة بينما يُعاد تشغيل تاريخه من أوله يبقى تعارضًا. **لا يُغلق بمقارنة محتوى**
لأننا لا نحتفظ بمحتوى المراجعات القديمة. الحل الصحيح بذر المؤشر من الـbootstrap،
وهو **غير ممكن بالعقد الحالي**: استجابة السحابة لا تحمل أي رقم تسلسل. يحتاج تغييرًا
في الخادم وقرارًا مستقلًا من المالك.

## درس عملية

`/code-review` قبل الدفع أخرج **ستة** أعطال حقيقية في يومين، كلها **بعد** أن راجعت
جلستان الكود سطرًا بسطر:
- إصلاح لا يعمل على الجهاز الذي كُتب من أجله (نمذجة أحد كاتبَين).
- إعلان نجاح بينما حاجز المزامنة لم يُرفع.
- ادعاء ضياع بيانات على جهاز سليم — كان سيدفع المالك لتكرار حركاته.
- تصنيف مبني على قائمة مقتطعة.
- صف مُقرّ ينجو من الاستعادة فيقفل الجهاز على خطأ لا يبلغه الإصلاح.
- حارس `namespace` ناقص، **مرتين في يوم واحد** على بوابتين متشابهتين.

البوابة قاعدة المشروع، وليست لأي جلسة صلاحية الإعفاء منها. وحين أُعفيت منها مرة،
وجدت المراجعة الخلل الأول من هذه القائمة.

**وملاحظة على الاختبارات نفسها:** ثلاث مرات كان الاختبار القائم يُثبّت **السلوك
المعطوب** بوصفه عقدًا (قفل الـtombstone، وفكستشر يؤرّخ الـcheckpoint والصفوف بنفس
اللحظة، وفكستشر بلا صفوف مُقرّة إطلاقًا). اختبارٌ يمرّ ليس دليلًا على صحة السلوك،
بل على أن السلوك يطابق ما توقّعه كاتبه.
